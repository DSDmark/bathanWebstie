import { Rubik, Exo_2, Inconsolata, Anton } from 'next/font/google'

export const rubik = Rubik({ subsets: ['latin'], variable: '--font-rubik' })
export const exo2 = Exo_2({ subsets: ['latin'] })
export const anton = Anton({ subsets: ['latin'], weight: '400', variable: '--font-anton' })
export const incon = Inconsolata({ subsets: ['latin'], weight: '400', variable: '--font-anton' })
