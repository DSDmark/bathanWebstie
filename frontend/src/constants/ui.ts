export const THEME_MODES = Object.freeze({
  light: 'light',
  dark: 'dark',
})

export const DEBOUNCE_DELAY_VALUE = 1000
export const DEFAULT_OPTIONS_VALUE = { id: '', name: '' }
export const PAGE_PER_ENTRY = [5, 10, 50, 100]
